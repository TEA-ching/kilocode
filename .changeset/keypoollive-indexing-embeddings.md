---
"kilo-code": minor
---

Add KeyPool Live as a codebase-indexing embedding provider, so vault-backed embedding models (e.g. Mistral's codestral-embed, Cohere's embed-multilingual-v3.0) can be used for indexing with the same key rotation (including Aggressive Rotation) already used for chat.
